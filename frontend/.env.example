# Backend API URL (required for production).
# Local dev: use http://localhost:3001
# Production: set in DigitalOcean App Platform to your backend's PUBLIC_URL (e.g. https://backend-xxxxx.ondigitalocean.app)
# Copy to .env and adjust.
VITE_API_URL=http://localhost:3001

# Development only: bypass login UI and accept a dummy token. Set back to false for production builds.
VITE_BYPASS_LOGIN=true
